server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # API is routed by Kubernetes Ingress to backend; no proxy here
    location /api/ {
        return 404;
    }
}
